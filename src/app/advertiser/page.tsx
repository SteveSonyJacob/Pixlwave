import Link from "next/link";
import { requireIdentity } from "@/lib/auth/identity";

export default async function AdvertiserPage() {
  const identity = await requireIdentity();
  return <main className="dashboard-shell"><div className="dashboard-heading"><div><span className="eyebrow">Advertiser mode</span><h1>Campaign workspace</h1><p>Welcome, {identity.fullName}. Discover Kerala inventory, prepare a cart and follow booking decisions.</p></div><Link className="button button-secondary button-small" href="/account">Switch mode</Link></div><div className="dashboard-grid"><section className="panel-card"><h2>Cart and requests</h2><p>Group current quotes and select an approved creative version. Online payment and cart submission are still being integrated.</p><div className="review-actions"><Link className="button button-small" href="/cart">Open cart</Link><Link className="button button-secondary button-small" href="/bookings">Booking status</Link></div></section><section className="panel-card"><h2>Creative library</h2><p>Upload PNG, JPEG, MP4 or WebM assets. Cart lines pin one clean version and its metadata.</p><Link className="button button-secondary button-small" href="/advertiser/creative">Manage creatives</Link></section></div><div className="truth-note"><b>Booking policy</b><span>Payment received — awaiting admin confirmation</span><small>Once payment is available, paying will start review but will not reserve inventory. Only admin approval does that.</small></div></main>;
}
